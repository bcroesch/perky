  <%= form_for([@account,@user], html: { class: "form-horizontal" }) do |f| %>
    <fieldset>
      <div class="row-fluid">
        <div class="col-md-6">
          <div class="control-group">
            <%= f.label :email, class: "control-label" %>
            <div class="controls">
              <%= f.email_field :email, autofocus: true, class: "input-xlarge" %>
            </div>
          </div>

          <div class="control-group">
            <%= f.label :monthly_credits, class: "control-label" %>
            <div class="controls">
              <%= f.number_field :monthly_credits, class: "input-xlarge" %>
            </div>
          </div>

          <div class="control-group">
            <div class="controls">
              <%= f.submit 'Create', class: "btn btn-primary" %>
            </div>
          </div>
        </div>
      </div>
    </fieldset>
  <% end %>
