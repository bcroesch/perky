<h3>Users</h3>
<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= link_to user.first_name, account_user_path(@account.id, user.id) %> signed up <%= user.created_at.to_date %>
    </li>
  <% end %>


</ul>
<div>
  <%= link_to 'Add new user', new_account_user_path %>
</div>
